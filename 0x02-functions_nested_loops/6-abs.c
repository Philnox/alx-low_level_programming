#include "main.h"

/**
 * _abs - checks number
 * @r: integer receipt by main.
 * Return: Returns r when it is positive absolute value,
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
